#!/bin/sh
# Maven wrapper - downloads Maven if not installed
# Usage: ./mvnw spring-boot:run

MAVEN_VERSION=3.9.5

if ! command -v mvn >/dev/null 2>&1; then
  echo "Maven not found. Please install Maven 3.6+ or use: mvn spring-boot:run"
  exit 1
fi

mvn "$@"
